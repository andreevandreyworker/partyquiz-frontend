# partyquiz-frontend

Frontend (React + TS + Vite) — mobile-first PWA-style party quiz UI, i18n (ru/en), served by nginx.

Part of the **PartyQuiz** project. Built and published to `ghcr.io/andreevandreyworker/partyquiz-frontend` via CI on push to `main` and on version tags.
